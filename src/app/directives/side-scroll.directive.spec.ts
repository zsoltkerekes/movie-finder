import { SideScrollDirective } from './side-scroll.directive';

describe('SideScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SideScrollDirective();
    expect(directive).toBeTruthy();
  });
});
